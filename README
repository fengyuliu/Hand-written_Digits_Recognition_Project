##
The code should be run in linux (ubuntu)

# Task 1
Run the SVM_Kernel.m in Matlab
Click the run button. You will be asked to input a number to choose PCA or LDA, and to choose different types of Kernels.
Just type the number: 1 or 2
Others are function files



# Task 2
Copy the folder "Part1_Task2" to the root location of caffe
cd $CAFFE_ROOT ./Part1_Task2/Run_MNIST_LeCun.sh
Change the max iteration number in MNIST_LeCun_solver.prototxt
The final model for 5000 iteration is attached in the folder
